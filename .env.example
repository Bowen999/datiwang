# Supabase 配置（可选）
# 在 https://supabase.com 创建项目后，从 Project Settings -> API 获取以下两项。
# - 联机对战：只需 Realtime 功能，无需建表；
# - 全局访问统计：需在 SQL Editor 运行 scripts/supabase-setup.sql 创建 visit_events 表。
# 不配置时自动使用「本地演示模式」（同一浏览器的多个标签页之间联机，统计仅存本机）。

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=